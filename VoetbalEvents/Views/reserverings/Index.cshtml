@model IEnumerable<VoetbalEvents.Models.reservering>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Datum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Wedstrijd)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Datum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Wedstrijd.Naam)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ReserveringID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ReserveringID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ReserveringID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
